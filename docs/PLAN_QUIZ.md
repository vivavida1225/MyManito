# 팀별 비밀 퀴즈 구현 계획

## 1. 요약

- 신규 Django 앱 `apps.quizzes`에 질문 시드, 팀 설정, 회차, 참가자별 퀴즈, 답안·평가·정산 상태를 분리해 구현한다.
- 기존 팀 관계, 리더보드 `ScoreEvent`, 알림·웹푸시, 실시간 갱신, 팀 종료·7일 삭제 흐름을 확장한다.
- 서버가 모든 기간 전환을 결정하며, APScheduler가 매분 처리하되 DB 잠금과 유니크 제약으로 재시도·다중 인스턴스 중복을 방지한다.
- 구현 순서는 스키마·시드 → 상태 전환·정산 → API·팀 통합 → Vue UI·에셋 → 전체 검증으로 한다.

## 2. 데이터와 상태 전환

### 핵심 모델

- `SystemQuizQuestion`
  - `stable_id`, 원본 번호, 분류, 질문 본문, 활성 상태, 표시 순서를 저장한다.
  - 삭제 대신 비활성화해 과거 배정 이력과 질문 스냅샷을 보존한다.
- `TeamQuizSettings`
  - Team과 1:1로 연결하고 `enabled=false`, 입력 2일, 풀이 3일을 기본값으로 둔다.
  - `quiz_timezone`, `next_round_starts_at`, 다음 공통 질문, 최초 전원 Claim 알림 시각을 저장한다.
  - 시간대는 첫 회차 시작 후 잠그고, OFF 전환 시 다음 회차 예약만 제거한다.
- `QuizRound`
  - 팀별 순번과 시작 시각을 유니크하게 저장한다.
  - 시간대·기간·입력/풀이/평가 마감 시각을 스냅샷한다.
  - 상태는 `ACTIVE`, `SETTLED`, `CANCELLED`, 충돌 결정은 `NOT_REQUIRED`, `PENDING`, `PROCEED`, `CANCEL`로 관리한다.
- `QuizItem`
  - 회차별 기준 답안 작성자와 서버가 관계에서 계산한 풀이자를 저장한다.
  - 질문 종류·ID·본문 스냅샷, 기준 답안, 마지막 유효 풀이 임시본, 자동 제출본, 평가점수, 정산 결과를 보관한다.
  - 정산 종류는 `REFERENCE_MISSING`, `SOLUTION_MISSING`, `EVALUATED`, `EVALUATION_MISSING`으로 고정한다.
  - `raw_score`와 퀴즈 점수율 분모 기여값 `rate_max_score`를 각각 저장한다. 기준 답안 미입력은 원점수 3, 최대점수 0으로 기록한다.
- 기존 `ScoreEvent`
  - 퀴즈 풀이자 결과와 기준 답안 작성자 조정 이벤트 유형을 추가한다.
  - `points`를 signed integer로 변경하고 `requested_points`, `reason`, nullable `quiz_item`을 추가한다.
  - `(quiz_item, event_type)` 유니크 제약으로 역할별 점수 이벤트를 한 번만 생성한다.

### 회차 처리

- 활성화 시 관리자 브라우저 IANA 시간대를 저장하고, 정확히 다음 현지 날짜 00:00을 첫 시작으로 예약한다.
- 회차 생성 트랜잭션에서 설정과 팀을 잠그고 참가자별 `QuizItem`을 한 번에 만든다.
  - 공통 질문은 전원에게 배정한 뒤 설정에서 비운다.
  - 시스템 질문은 참가자별 과거 이력을 제외하고 무작위 고정한다.
  - 공통 질문과 시스템 질문 사이에서도 정규화 본문이 같은 질문은 재배정하지 않는다.
  - 한 명이라도 선택 가능한 시스템 질문이 없으면 회차 전체를 만들지 않고 모드를 OFF로 전환한다.
- 풀이 임시 저장은 공백만으로 기존 유효본을 지우지 않는다. 마감 시 마지막 비어 있지 않은 저장본만 자동 제출한다.
- 마감 처리 순서는 다음과 같이 고정한다.
  1. 입력 마감: 미결정 충돌 회차 취소 또는 정상 풀이 시작 알림.
  2. 풀이 마감: 자동 제출 후 기준 답안 미입력·풀이 미제출 항목 즉시 정산, 제출 항목은 평가 단계로 전환.
  3. 평가 확정: 즉시 정상 정산.
  4. 평가 마감: 남은 항목을 자동 2점 및 작성자 감점으로 정산.
- 감점은 Participant를 잠근 뒤 `max(0, 현재 점수 + 요청 감점)`으로 계산하고 실제 증감과 요청값을 함께 기록한다.
- QUIZ_SCORE_MULTIPLIER는 서버 설정으로 두며 기본값 3을 별도 환경 설정 없이 사용한다.

### 팀 기능 통합

- 마지막 Claim 완료 트랜잭션에서 관리자 준비 알림을 한 번만 생성한다.
- Claim 초기화는 퀴즈가 OFF이고 미정산 회차가 없을 때만 허용한다.
- 종료 예정일 저장은 `quiz_timezone` 자정 기준으로 충돌을 다시 계산한다.
  - 입력기간 중 새 충돌은 회차를 `PENDING`으로 바꾸고 관리자에게 알린다.
  - 이미 풀이 중인 회차와 충돌하면 확인 없는 저장을 `409 QUIZ_COLLISION_CONFIRMATION_REQUIRED`로 거부하고, 확인 플래그를 포함한 재요청만 허용한다.
- 팀 조기 종료 트랜잭션은 이미 지난 마감부터 먼저 정산하고, 아직 마감되지 않은 모든 항목은 점수 없이 취소한 뒤 최종 리더보드 스냅샷을 생성한다.
- 종료 7일 후 기존 Team cascade 삭제로 모든 팀별 퀴즈 원문·점수율·이력을 함께 삭제하고 전역 시스템 질문만 유지한다.

## 3. API와 화면

### API 계약

| API | 동작 |
|---|---|
| `GET/PATCH /api/teams/{code}/admin/quiz/` | 관리자 설정, 다음 시작·충돌 미리보기, 참가자별 제출 상태를 조회·변경한다. 답안 원문이나 역할 간 관계는 반환하지 않는다. |
| `POST /api/teams/{code}/admin/quiz/rounds/{round_id}/decision/` | `PROCEED` 또는 `CANCEL`을 마감 전 한 번만 확정한다. |
| `GET /api/teams/{code}/quiz/` | 현재 역할별 할 일과 본인이 풀이자로 참여한 정산 완료 이력만 반환한다. |
| `POST /api/teams/{code}/quiz/items/{id}/reference-answer/confirm/` | 기준 답안을 불변 확정한다. 동일 내용 재요청은 기존 결과, 다른 내용은 409를 반환한다. |
| `PUT /api/teams/{code}/quiz/items/{id}/solution-draft/` | 풀이기간 중 마지막 유효 임시 답안을 저장한다. 공백 요청은 기존 유효본을 유지한다. |
| `POST /api/teams/{code}/quiz/items/{id}/evaluation/confirm/` | 1~5점을 불변 확정한다. 동일 점수 재요청만 멱등 허용한다. |

- 모든 변경 API는 클라이언트의 Participant 식별값을 받지 않고 현재 사용자와 서버 관계에서 역할을 계산한다.
- 평가 응답에는 풀이자의 이름, 사용자 ID, Participant ID, 프로필을 넣지 않는다.
- 기준 답안은 풀이기간에 노출하지 않고, 풀이자는 정산 후에만 기준 답안과 원점수를 받는다.
- 리더보드 API는 결과 공개 전 퀴즈 점수율을 누구에게도 포함하지 않는다. 결과 공개 후 각 entry에 소수점 한 자리 숫자 또는 산출 불가를 뜻하는 `null`을 추가한다.
- 실시간 이벤트는 `quiz.changed`와 `team_code`만 전달하고 클라이언트가 REST를 다시 조회하게 한다.

### 알림

- 알림 종류를 `QUIZ_READY`, `QUIZ_REFERENCE_OPEN`, `QUIZ_SOLVE_OPEN`, `QUIZ_EVALUATION_OPEN`, `QUIZ_END_CONFLICT`, `QUIZ_POOL_EXHAUSTED`, `QUIZ_ROUND_CANCELLED`로 확장한다.
- nullable unique `dedupe_key`로 회차·이벤트·수신자별 인앱 알림을 한 번만 생성한다.
- 인앱 저장 후 커밋 콜백에서 웹푸시와 실시간 갱신을 보내며, 푸시 실패는 인앱 생성에 영향을 주지 않는다.
- 푸시와 알림 데이터에는 질문·답안·평가 원문 및 참가자 식별자를 포함하지 않는다.
- 기준 답안 확정이나 각 작업 마감 시 관련 알림만 읽음 처리한다.
- 알림 목록 응답에 `has_pending_quiz_reference_answer`를 추가해 전체 읽음 여부와 독립적인 Navbar 강조 조건으로 사용한다.

### Vue UI

- `/teams/:teamCode/quiz`에 참가자용 `TeamQuizView`를 추가한다.
  - 기준 답안, 풀이 임시 저장, 직전 답안 평가, 본인 결과를 역할별 카드로 표시한다.
  - 기준 답안·평가는 확인 모달을 거쳐 확정하고, 정확한 리더보드 증감값은 표시하지 않는다.
- `/teams/:teamCode/admin/quiz`에 관리자 설정 화면을 추가한다.
  - ON/OFF, 시간대, 입력·풀이일수, 다음 공통 질문, 다음 시작·충돌 미리보기, 참가자별 상태, 충돌 결정을 제공한다.
  - 참가자 상태는 각 참가자의 역할별 완료 여부만 보여주고 답안 원문과 상대 관계는 숨긴다.
- TeamHome에는 Claim된 참가자만 비밀 퀴즈 메뉴를, 관리자 대시보드에는 설정 메뉴를 추가한다.
- 종료 결과 화면에서도 보존기간 동안 개인 퀴즈 이력으로 이동할 수 있게 하되, 전체 점수율은 결과 공개 이후 리더보드에서만 보여준다.
- 알림 CTA는 관리자 이벤트를 관리자 퀴즈 화면으로, 참가자 이벤트를 참가자 퀴즈 화면으로 연결한다.

## 4. 자동 처리, 시드와 에셋

- 기존 APScheduler에 매분 실행되는 단일 퀴즈 타임라인 작업을 등록한다.
  - 지난 마감 처리 후 새 회차 생성 순서로 실행한다.
  - 각 후보를 별도 원자적 트랜잭션에서 다시 조회·잠그고, 저장된 마감 시각으로 처리한다.
  - API 요청과 자동 작업이 경합하면 동일 레코드 잠금을 먼저 획득한 트랜잭션만 상태를 확정한다.
  - 모든 알림과 `quiz.changed` 이벤트는 커밋 이후 발행한다.
- `tmp/quizes_raw.md` 변환기는 UTF-8을 명시해 읽고 다음 규칙으로 추적 가능한 JSON 시드를 생성한다.
  - `### ` 헤더와 `숫자. 질문`을 파싱하며 빈 줄과 `---`만 무시한다.
  - 분류·문항 수를 하드코딩하지 않고, 번호 연속성·중복·빈 본문·대체문자·분류 없는 문항을 검증한다.
  - ID는 `SYSTEM_{number:03d}`로 만들고 본문을 수정하지 않은 채 `ensure_ascii=False`, UTF-8로 기록한다.
  - 현재 원천의 초기 결과는 4개 분류·50문항이며, 이후 의도적인 원천 변경은 시드 재생성과 검증된 메타데이터 갱신으로 수용한다.
- 멱등 관리 명령이 stable ID 기준으로 upsert하고 시드에서 빠진 기존 시스템 질문은 삭제하지 않고 비활성화한다.
- Compose 시작 순서에 migrate → quiz seed → collectstatic → Daphne를 적용해 배포 환경이 `tmp/`에 의존하지 않게 한다.
- 원본 `alertEmphasize.png`는 byte 단위로 보존한다. 제작자 표기와 불필요한 여백만 제외해 작은 WebP로 변환하고 `frontend/src/assets/alertEmphasize.webp`를 50KB 미만으로 검증한다.
- Navbar 알림 버튼 왼쪽 아래에 절대 배치하고 `transform: scale()` 기반 `ease-in-out` 펄스를 적용한다. `prefers-reduced-motion: reduce`에서는 애니메이션만 중지한다.

## 5. 검증과 배포

- 백엔드 자동 테스트:
  - 유연한 파서의 현재 4/50 결과, 원문·ID 보존, 반복 시드 멱등성, 잘못된 번호·UTF-8·중복 거부.
  - Claim 조건, 시간대/DST 자정, 기간 경계의 반개구간, 설정 스냅샷, OFF·재활성화.
  - 공통/시스템 질문 중복 금지, 풀 소진, 회차·배정 동시 생성 중복 방지.
  - 답안·평가 불변성과 멱등성, 공백 임시 저장 시 직전 유효본 유지.
  - 네 가지 정산표, 감점 0점 제한, 요청/실제 감점 감사값, 중복 점수 방지, 점수율 포함·제외 규칙.
  - 충돌 경계 `Q_START <= E < Q_END`, 미응답 취소, 종료 예정일 경고, 조기 종료 catch-up/취소.
  - 알림 수신자·dedupe·읽음/할 일 분리·푸시 실패 격리·원문 미포함.
  - 관리자 원문 차단, 평가자 신원 차단, REST·이력·알림·실시간 이벤트의 익명성 및 기존 `counterpart_name=null` 회귀 테스트.
  - 결과 공개 전 점수율 비공개, 공개 후 한 자리 반올림/null, 종료 7일 cascade 삭제.
- PostgreSQL `TransactionTestCase`로 자동 작업 재시도와 동시 정산을 검증한다.
- 프론트엔드는 프로덕션 빌드와 모바일 화면 수동 시나리오로 네 역할 카드, 확인 모달, 실시간 갱신, 충돌 UX, 알림 이동, 펄스 및 reduced-motion을 검증한다.
- 완료 명령은 전체 Django 앱 테스트, `makemigrations --check`, `check`, `npm run build`, WebP 파일 크기 검사로 구성한다.
- 배포 전 PostgreSQL 백업을 남기고 마이그레이션·시드 성공, 활성 시스템 질문 50개, 스케줄러 등록, 중복 점수·알림 부재를 확인한다.
- 기존 dirty worktree의 README, 명세, 로그인 스크립트 등 비관련 변경은 건드리지 않는다. 새 텍스트·JSON·코드와 시드 생성기는 모두 명시적 UTF-8로 처리한다.
